For connecting GDB...

// Set symbol & exec to <filename>.elf
arm-none-eabi-gdb --se=build-APARD/firmware.elf

// connect to remote on local machine TCP port :3333
target extended-remote localhost:3333

// reset halt the MCU
monitor reset halt

// set a breakpoint on main & hit it
b main
continue

// now...time to mess around!
